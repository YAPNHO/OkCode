# OkCode 第一阶段：流式多轮对话 Spec

## 背景

OkCode 是一个从零构建的终端 AI 编程助手，长期目标是发展成类似 Claude Code 的 Coding Agent。当前项目尚无实现代码。

本阶段先建立最小可用的纯对话基础：完成终端交互、模型接入、流式输出和多轮上下文，为后续工具调用、文件操作与代码编辑提供稳定基础。

DeepSeek API 的 `deepseek-v4-pro` 是第一版主要真实模型，用于开发调试和端到端验收。

## 目标

- 用户启动 OkCode 后，进入保留普通终端滚动历史的行内对话界面。
- 支持 Anthropic 和 OpenAI 两种协议，并允许接入遵循相应协议的兼容服务。
- 通过 OpenAI Chat Completions 协议接入 DeepSeek V4 Pro。
- 模型生成期间持续展示增量内容，无需等待完整回复。
- Claude 启用 extended thinking 时，分别流式展示 API 返回的思考内容和正式回答。
- OpenAI 兼容服务启用 thinking 时，分别流式展示 `reasoning_content` 和正式回答；第一版不设置 reasoning 强度参数。
- 在本次运行期间维护多轮上下文，使模型能够理解此前对话。
- 建立统一的供应商能力边界，后续新增其他模型协议时无需改动对话界面的核心行为。

## 功能需求

- **F1：启动与交互**  
  用户启动 OkCode 后进入行内终端对话界面。终端原有滚动历史得到保留，界面显示当前配置名和模型；用户可以连续提交文本问题并正常退出程序。

- **F2：YAML 配置**  
  OkCode 启动时读取 YAML 配置。配置包含顶层 `active` 字段和供应商配置列表；`active` 必须引用列表中唯一的 `name`。每项供应商配置包含 `name`、`protocol`、`model`、`base_url`、`api_key`，以及可选布尔字段 `thinking`。  
  `protocol` 第一版仅接受 `anthropic` 或 `openai`；`thinking` 省略时视为关闭。配置错误时必须给出明确提示，并且不得发起 API 请求。

- **F3：供应商切换与协议兼容**  
  每次启动根据 `active` 选择一个供应商配置。修改配置并重新启动后即可切换后端，不支持运行时热切换。自定义 `base_url` 可以指向遵循相应协议的第三方兼容服务。OpenAI 适配器支持可选的 thinking 能力扩展，但不按模型名或 `base_url` 硬编码特定供应商；不支持该扩展的服务应返回明确请求错误。

- **F4：统一供应商能力**  
  对话界面以一致方式使用不同供应商，不直接依赖某一家协议的事件格式。第一版 Anthropic 使用 Messages 协议，OpenAI 使用 Chat Completions 协议；OpenAI 协议的可选 `reasoning_content` 事件与 Anthropic thinking 事件都映射为统一思考增量。以后新增协议时，不应改变终端对话的基本交互流程。

- **F5：SSE 流式回复**  
  模型生成过程中，OkCode 持续消费 SSE 流并在收到文本增量后立即追加显示，形成逐步输出的效果，不等待完整响应返回。一次 SSE 增量可能包含多个字符，不要求网络事件与单个字符一一对应。

- **F6：Extended Thinking 与兼容推理内容**  
  当 Anthropic 配置的 `thinking` 为 `true` 时，OkCode 使用内置的合法预算启用 extended thinking，并将 API 返回的思考内容与正式回答分区、分别流式展示。当 OpenAI 兼容配置的 `thinking` 为 `true` 时，OkCode 请求供应商启用其兼容 thinking 扩展，将流中的 `reasoning_content` 与正式回答分区、分别流式展示，不传递 reasoning 强度参数。普通多轮对话只向 OpenAI 协议回传正式回答，不回传上一轮 `reasoning_content`。

- **F7：进程内多轮对话**  
  OkCode 在当前运行期间保留所有已完成的用户消息和助手响应，并在下一轮请求中提供必要上下文。退出程序后会话清空，不写入本地文件，也不支持恢复历史会话。

- **F8：错误、中断与退出**  
  API 请求失败或流式连接中断时，界面显示可理解且不包含密钥的错误信息，并返回输入状态，不因单次失败直接退出。用户可以中断正在生成的回复；未完整生成的助手回复不加入后续对话历史。空输入不发起请求，用户可以通过明确的退出操作结束程序。

## 非功能需求

- **N1：流式响应及时性**  
  OkCode 收到 SSE 内容增量后应立即刷新终端，不进行人为批量缓存；模型或网络本身的延迟不计入 OkCode 的显示延迟。

- **N2：终端兼容性**  
  第一版应能在 Windows Terminal/PowerShell 和常见 Linux 终端中正常运行，正确显示中文、换行及基本终端样式；终端宽度变化不得导致程序崩溃。

- **N3：交互稳定性**  
  正常退出、请求失败或用户中断生成后，终端应恢复到可继续输入或已正常退出的状态，不留下损坏的输入行、未结束的加载状态或异常堆栈。

- **N4：敏感信息保护**  
  `api_key` 不得出现在欢迎信息、普通输出或错误信息中。对底层异常进行展示时，应避免泄漏请求认证信息。

- **N5：可扩展性**  
  供应商协议差异应被限制在独立边界内。后续增加新协议时，不需要重写配置加载、会话管理或终端交互流程。

- **N6：可测试性**  
  配置选择、事件转换、流式显示和多轮历史等核心行为应能在不访问真实 API 的情况下自动验证；最终还需在 tmux 中使用真实对话完成端到端验收。

- **N7：失败可诊断性**  
  配置错误、认证失败、连接失败、服务端错误和流式中断应向用户显示可区分的简明错误信息；默认不向普通用户输出完整内部堆栈。

## 不做的事

本阶段明确不包含：

- Tool Use、函数调用、Shell 命令执行。
- 文件读取、目录搜索、代码编辑与补丁应用。
- 会话持久化、历史会话列表和退出后的会话恢复。
- 运行期间切换供应商、模型或热加载配置。
- 全屏 TUI、鼠标操作、固定面板或复杂布局。
- 图片、音频、文件附件及其他多模态输入。
- 多个模型请求并发执行；生成期间只允许等待或中断当前请求。
- 自动压缩、裁剪或总结过长的会话上下文；达到模型限制时按 API 错误处理。
- 自动探测模型是否支持 extended thinking，或适配 thinking 扩展之外的第三方私有协议差异。
- 完整 Markdown 重排、代码语法高亮等高级渲染；第一版保证模型文本和换行能够持续、正确显示。
- API 密钥加密、系统钥匙串管理以及环境变量占位符；第一版从本地 YAML 配置读取密钥。
- Token 用量统计、费用计算、日志平台和遥测上报。
- 自动重试、故障转移以及在多个供应商之间自动切换。

## 验收标准

- **AC1（F1）**：启动 OkCode 后，可以看到当前配置名、模型和输入提示；提交问题后进入生成流程，终端原有滚动历史仍然可用。
- **AC2（F2、F3）**：YAML 中存在多个供应商配置时，程序只使用 `active` 指向的配置；修改 `active` 并重新启动后，请求切换到新的供应商。
- **AC3（F2）**：缺少必填字段、配置名重复、`active` 不存在、协议值非法或字段类型错误时，程序显示具体配置错误，并且没有请求任何模型服务。
- **AC4（F3、F4）**：分别选择 OpenAI 和 Anthropic 配置时，都能通过相同的终端对话流程完成提问和流式回复；自定义 `base_url` 能收到符合所选协议的请求。
- **AC5（F5）**：让测试服务分批延迟发送至少三个文本增量时，终端在流结束前就按顺序显示前面的增量，不等待完整响应。
- **AC6（F6）**：Anthropic 配置为 `thinking: true` 时，测试流中的 thinking 和 text 增量分别出现在“思考”和“回答”区域，并且均在各自流结束前开始显示。OpenAI 兼容配置为 `thinking: true` 时，测试流中的 `reasoning_content` 和 `content` 也分别出现在两个区域。
- **AC7（F6）**：Anthropic 配置省略 `thinking` 或设为 `false` 时，不启用 extended thinking；OpenAI 兼容配置为 `thinking: true` 时，请求包含 enabled thinking 扩展但不包含 reasoning 强度参数。服务不支持该扩展时，显示请求错误。
- **AC8（F7）**：同一进程中先提供一个仅本轮可知的信息，再在下一轮追问，模型能够根据发送的历史回答；检查第二次请求可以看到此前已完成的用户消息和助手回复。
- **AC9（F7）**：退出并重新启动后发起追问，新请求中不包含上一次运行的消息。
- **AC10（F8）**：认证失败、连接失败、服务端错误或 SSE 流中断后，用户能看到可区分的简明错误，并可继续提交下一轮问题。
- **AC11（F8）**：中断正在生成的回复后，程序停止消费当前流并返回输入状态；下一轮请求不包含被中断的助手回复。
- **AC12（F8）**：提交空输入不会产生 API 请求；执行退出操作后程序正常结束且终端状态可继续使用。
- **AC13（N4）**：使用带有唯一标记的测试密钥触发各种错误后，欢迎信息、普通输出和错误输出中均找不到该密钥。
- **AC14（N2、N3）**：在 Windows Terminal/PowerShell 和 Linux tmux 中分别完成启动、中文提问、流式回复、中断与退出，中文和换行正常，程序没有异常堆栈或残留状态。
- **AC15（N5、N6）**：使用不访问真实网络的可控测试后端，可以自动验证配置选择、两种协议的事件转换、流式顺序和多轮历史。
- **AC16（N6）**：在 tmux 中连接 DeepSeek `deepseek-v4-pro`，完成“启动 → 连续两轮相关提问 → 观察思考和回答的流式回复 → 正常退出”的端到端场景。
